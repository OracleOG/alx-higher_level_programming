#!/usr/bin/python3


def best_score(a_dictionary):
    list_keys = list(a_dictionary.keys())

    for i in list_keys:
        for x in list_keys:
            if a_dictionary[i] >= a_dictionary
            best = i
            break

    return (best)
